// DEMO FIXTURE — clean, pure functions only. No network, fs, shell, or secrets.
export function celsiusToFahrenheit(c) {
  return c * (9 / 5) + 32;
}
export function kmToMiles(km) {
  return km * 0.621371;
}
export function kgToPounds(kg) {
  return kg * 2.20462;
}
